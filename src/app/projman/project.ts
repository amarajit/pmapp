export interface IProject{
    id: string,
    project : string,
    startDate : string,
    endDate : string,
    priority : string,
    managerId : string,
    taskCount : number,
    completedTaskCount :number
}